extends layout

block content
  div(style="display:flex; flex-direction:column; grid-column: span 2; text-align:center") 
    h1 Oops! Page Not Found
    p(style="font-size: 18px") The page you are looking for might have been removed, had its name changed, or is temporarily unavailable.
    a(href="/" style="font-size: 21px") Back home
  //- h1= message
  //- h2= error.status
  //- pre #{error.stack}
